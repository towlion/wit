# Frontend

This directory is an optional placeholder for a Next.js frontend.

## Getting Started

To scaffold a Next.js app here:

```bash
npx create-next-app@latest frontend --typescript --tailwind --app
```

The frontend communicates with the backend via `/api`. See the [platform spec](https://github.com/towlion/platform/blob/main/docs/spec.md) for details.
